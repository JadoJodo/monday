# GoReleaser configuration for rundown.
# Docs: https://goreleaser.com
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: rundown
    main: .
    binary: rundown
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/JadoJodo/rundown/cmd.version={{.Version}}
      - -X github.com/JadoJodo/rundown/cmd.commit={{.Commit}}

universal_binaries:
  - id: rundown
    replace: true

archives:
  - id: rundown
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

homebrew_casks:
  - name: rundown
    repository:
      owner: JadoJodo
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/JadoJodo/rundown"
    description: "Automate routine macOS maintenance from one command"
    # Strip the macOS quarantine flag so the freshly-downloaded binary runs
    # without a Gatekeeper prompt.
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/rundown"]
          end
